const baseURL = "https://services.intracircum.ca";
const EPC = {
  EPC: baseURL + "/api",
  Resume: baseURL + "/api/resume",
  IP: baseURL + "/api/ip",
  Messages: baseURL + "/api/messages",
};

export default EPC;
